# Environment Configuration

# Server
NODE_ENV=development
PORT=3000

# Cache Configuration
CACHE_PROVIDER=memory  # Options: memory, upstash
CACHE_DEFAULT_TTL=300  # Default TTL in seconds (5 minutes)

# Upstash Redis (only needed if CACHE_PROVIDER=upstash)
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token

# CORS
CORS_ORIGIN=*

# OpenTelemetry (optional)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces

# Vercel Environment Variables (automatically set by Vercel)
# VERCEL_ENV=production
# VERCEL_URL=your-app.vercel.app
# VERCEL_REGION=iad1
